# Диапазоны, чтобы на любом Python 3.9–3.13 нашлись готовые wheels.
# NetworkX — чистый Python; версия зафиксирована, чтобы Louvain давал те же кластеры.
pandas>=2.2,<3
numpy>=1.26,<3
pyarrow>=15
networkx==3.2.1
